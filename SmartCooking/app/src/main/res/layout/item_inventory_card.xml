<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cardInventoryItem"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="6dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?android:attr/selectableItemBackground"
    app:cardCornerRadius="20dp"
    app:cardElevation="2dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Phần hiển thị ảnh nền màu nhạt và Emoji -->
        <FrameLayout
            android:id="@+id/layoutEmojiBg"
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:background="@color/primary_light">

            <TextView
                android:id="@+id/tvItemEmoji"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="🍎"
                android:textSize="44sp" />

            <!-- Nhãn hạn sử dụng ở góc trên bên phải -->
            <TextView
                android:id="@+id/tvItemStatusBadge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="top|end"
                android:layout_margin="8dp"
                android:background="@drawable/bg_badge_fresh"
                android:paddingHorizontal="8dp"
                android:paddingVertical="3dp"
                android:text="Tươi mới"
                android:textColor="@color/white"
                android:textSize="9sp"
                android:textStyle="bold" />

            <!-- Phân loại thực phẩm ở góc trên bên trái -->
            <TextView
                android:id="@+id/tvItemCategoryBadge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="top|start"
                android:layout_margin="8dp"
                android:background="@drawable/bg_category_badge"
                android:paddingHorizontal="8dp"
                android:paddingVertical="3dp"
                android:text="Rau củ"
                android:textColor="@color/white"
                android:textSize="9sp"
                android:fontFamily="sans-serif-medium" />
        </FrameLayout>

        <!-- Phần thông tin chi tiết -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="12dp">

            <RelativeLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content">

                <TextView
                    android:id="@+id/tvItemName"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_toStartOf="@id/btnItemMore"
                    android:fontFamily="sans-serif-bold"
                    android:textColor="@color/text_primary"
                    android:textSize="14sp"
                    android:maxLines="1"
                    android:ellipsize="end"
                    android:text="Cà chua bi" />

                <ImageView
                    android:id="@+id/btnItemMore"
                    android:layout_width="20dp"
                    android:layout_height="20dp"
                    android:layout_alignParentEnd="true"
                    android:layout_centerVertical="true"
                    android:src="@drawable/ic_more_vert"
                    app:tint="@color/text_secondary"
                    android:contentDescription="More options"
                    android:clickable="true"
                    android:focusable="true" />
            </RelativeLayout>

            <TextView
                android:id="@+id/tvItemQuantity"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="2dp"
                android:textColor="@color/text_secondary"
                android:textSize="12sp"
                android:text="Số lượng: 200g" />

            <!-- Thanh tiến độ hạn sử dụng trực quan -->
            <ProgressBar
                android:id="@+id/pbItemExpiry"
                style="@android:style/Widget.ProgressBar.Horizontal"
                android:layout_width="match_parent"
                android:layout_height="4dp"
                android:layout_marginTop="8dp"
                android:progress="80"
                android:progressTint="@color/success" />
        </LinearLayout>
    </LinearLayout>
</androidx.cardview.widget.CardView>
